{
  "private": true,
  "scripts": {
    "build": "rimraf dist && pnpm -r run build",
    "export": "pnpm -r run export"
  },
  "devDependencies": {
    "pnpm": "catalog:",
    "rimraf": "catalog:"
  }
}